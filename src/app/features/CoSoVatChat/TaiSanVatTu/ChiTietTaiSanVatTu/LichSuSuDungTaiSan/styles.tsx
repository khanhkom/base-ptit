import { StyleSheet } from 'react-native';

// config
import { HEIGHT } from '@common';

const styles = StyleSheet.create({
  listContainer: { paddingBottom: HEIGHT(30), paddingTop: HEIGHT(12) },
});

export default styles;
