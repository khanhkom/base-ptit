import Animated from 'react-native-reanimated';

/**
 * Keep current position when changing properties AnimatedTransform.
 */
export const sharedTransformOrigin = (): Animated.AnimatedTransform => {
  'worklet';

  return [];
};
