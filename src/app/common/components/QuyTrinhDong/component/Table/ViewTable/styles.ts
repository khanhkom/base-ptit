import { StyleSheet } from 'react-native';

import { HEIGHT } from '@config/function';

const styles = StyleSheet.create({
  content: { paddingBottom: HEIGHT(30), paddingTop: HEIGHT(24) },
});

export default styles;
